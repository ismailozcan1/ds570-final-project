services:
  dashboard:
    build: .
    image: steam-user-segmentation-dashboard
    container_name: steam-user-segmentation-dashboard
    ports:
      - "8502:8502"
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
